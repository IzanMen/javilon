---
// No necesitas poner nada en la parte de frontmatter de Astro
---

<script>
    console.log("hola")
    document.addEventListener("scroll", function () {
        console.log("scroll")
        const arrow = document.querySelector(".arrow") as HTMLElement;
        const scrollPosition = window.scrollY;
        const threshold = window.innerHeight * 3; // 300vh en píxeles

        if (arrow) {
            if (scrollPosition >= threshold) {
                arrow.style.opacity = "0";
                arrow.style.pointerEvents = "none"; // Evita interacción
            } else {
                arrow.style.opacity = "1";
                arrow.style.pointerEvents = "auto";
            }
        }
    });
</script>
